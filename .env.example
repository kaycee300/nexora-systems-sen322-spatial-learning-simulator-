SKILLSCAPE_SECRET=change-me-for-local-dev

# Frontend URL used after Google login succeeds.
# Use your actual frontend URL, for example http://127.0.0.1:5500/frontend
FRONTEND_URL=http://127.0.0.1:5500/frontend

# Google OAuth login. Create these in Google Cloud Console.
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# SMTP email delivery for signup verification codes.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER= iwuhdeborah72@gmail.com
SMTP_PASS= jwzv dztd cjzj qibi
FROM_EMAIL= iwuhdeborah72@gmail.com
SMTP_USE_TLS=true
SMTP_USE_SSL=false
SMTP_TIMEOUT=10

# Set to true only when FastAPI is behind a trusted reverse proxy.
TRUST_PROXY_HEADERS=false
